import type {
  DomAdapter,
  MetricsOptions,
  MetricsSummary,
  NormalizedMetricsOptions,
  RenderMetric,
  TestingPluginOptions,
} from '@pokujs/dom';

export type ReactDomAdapter = DomAdapter;
export type ReactMetricsSummary = MetricsSummary;
export type ReactMetricsOptions = MetricsOptions;
export type ReactTestingPluginOptions = TestingPluginOptions;

export type {
  NormalizedMetricsOptions,
  RenderMetric,
};
